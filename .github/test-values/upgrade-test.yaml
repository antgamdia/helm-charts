# SPDX-FileCopyrightText: SUSE LLC
# SPDX-License-Identifier: Apache-2.0

# Minimal values for chart upgrade testing in k3s.
# Disables components not required for DB migration validation,
# and adjusts service types for a test cluster.

trento-wanda:
  enabled: false

trento-mcp-server:
  enabled: false

prometheus:
  enabled: false

trento-web:
  ingress:
    enabled: false

rabbitmq:
  service:
    type: ClusterIP
  # Static config: skip the Go-template expression in the default extraConfiguration.
  extraConfiguration: |
    listeners.tcp.default = 5672

global:
  trentoWeb:
    origin: "localhost"
  postgresql:
    postgresqlPassword: "trento-test"
  rabbitmq:
    auth:
      password: "trento-test"
